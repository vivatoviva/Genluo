const config = {
  // 启动端口
  port: 3000,
  // 数据库配置
  mysql: {
    host: '120.78.71.60',
    user: 'root',
    password: 'xianzai',
    database: 'personSite',
    HOST: 'localhost'    
  }
}

module.exports = config;
